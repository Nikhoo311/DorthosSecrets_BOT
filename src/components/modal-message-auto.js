const { ActionRowBuilder, ButtonBuilder, ButtonStyle, ModalBuilder, StringSelectMenuBuilder, TextInputBuilder, TextInputStyle, MessageFlags } = require("discord.js");
const { LabelBuilder } = require("@discordjs/builders");
const ms = require("ms");
const config = require("../../config/config.json");
const { hasOfficierRole } = require("../modules/stuff/players.js");
const { buildColorOptions } = require("../functions/utils/colorOptions.js");
const MIN_DURATION_MS = 60000;
function parseDuration(input) { const parts = [...input.matchAll(/(\d+(?:[.,]\d+)?)\s*(seconds?|secs?|s|minutes?|mins?|min|m|hours?|hrs?|hr|h|days?|d|weeks?|w|years?|yrs?|yr|y)/gi)]; return parts.length && parts.map((part) => part[0]).join("").replace(/\s/g, "") === input.replace(/\s/g, "") ? parts.reduce((total, part) => total + ms(`${part[1].replace(",", ".")}${part[2]}`), 0) : null; }
function parseStartAt(input) { if (!input) return null; const match = input.match(/^(\d{2})\/(\d{2})\/(\d{4}) (\d{2})h(\d{2})$/); if (!match) return null; const [, dayText, monthText, yearText, hourText, minuteText] = match; const day = Number(dayText), month = Number(monthText), year = Number(yearText), hour = Number(hourText), minute = Number(minuteText); const value = new Date(year, month - 1, day, hour, minute); return value.getFullYear() === year && value.getMonth() === month - 1 && value.getDate() === day && value.getHours() === hour && value.getMinutes() === minute ? value.getTime() : null; }
function modal(channelId) { const duration = new TextInputBuilder().setCustomId("duration").setStyle(TextInputStyle.Short).setPlaceholder("Ex: 2h30min, 1w").setRequired(true); const startAt = new TextInputBuilder().setCustomId("startAt").setStyle(TextInputStyle.Short).setPlaceholder("Ex: 24/08/2026 20h00").setRequired(false); const color = new StringSelectMenuBuilder().setCustomId("accentColor").setPlaceholder("Choisis une couleur").setMinValues(0).setMaxValues(1).setRequired(false).addOptions(buildColorOptions(config.color)); return new ModalBuilder().setCustomId(`modal-message-auto:${channelId}`).setTitle("Planifier un message automatique").addLabelComponents(new LabelBuilder().setLabel("Fréquence").setDescription("Minimum 1 min. Ex: 2h30min, 1w.").setTextInputComponent(duration),new LabelBuilder().setLabel("Date de début").setDescription("Vide : démarrage après la première fréquence.").setTextInputComponent(startAt),new LabelBuilder().setLabel("Couleur").setStringSelectMenuComponent(color)); }
module.exports={data:{name:"modal-message-auto",type:"modal",multi:"modal-message-auto"},parseDuration,MIN_DURATION_MS,showAutomaticMessageModal:(i,id)=>i.showModal(modal(id)),async execute(i){if(!hasOfficierRole(i.member,config.officerRoleIds))return i.reply({content:"❌ Accès refusé.",flags:MessageFlags.Ephemeral});const[,id]=i.customId.split(":"),durationInput=i.fields.getTextInputValue("duration").trim(),startInput=i.fields.getTextInputValue("startAt").trim(),durationMs=parseDuration(durationInput),startAt=parseStartAt(startInput),[colorName]=i.fields.getStringSelectValues("accentColor");if(!id||!Number.isFinite(durationMs)||durationMs<MIN_DURATION_MS||(startInput&&(!startAt||startAt<=Date.now())))return i.reply({content:"❌ Date invalide ou passée. Format : `24/08/2026 20h00`.",flags:MessageFlags.Ephemeral});const key=`${i.user.id}:${id}`;i.client.pendingAutomaticMessageCreations.set(key,{channelId:id,durationMs,durationInput,startAt,accentColor:config.color[colorName]??null});setTimeout(()=>i.client.pendingAutomaticMessageCreations.delete(key),900000).unref();await i.reply({content:"Planification enregistrée. Passe à la saisie du contenu.",components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId(`button-message-auto:create-content:${id}`).setLabel("Continuer").setStyle(ButtonStyle.Primary))],flags:MessageFlags.Ephemeral});}};
